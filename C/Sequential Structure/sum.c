#include <stdio.h>

int main(){

    int x , y, sum;

    printf("Enter the value of X: ");
    scanf("%d", &x);
    printf("Enter the valur of Y: ");
    scanf("%d", &y);

    sum = x + y;

    printf("SUM = %d", sum);

    return 0;
}